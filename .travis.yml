language: node_js
before_script:
    - sh -e /etc/init.d/xvfb start
    - git submodule update --init
    - cd addon-sdk
    - . bin/activate
    - cd ../
    - /usr/local/bin/firefox -v
env:
    - DISPLAY=':99.0' HOME=/tmp LD_LIBRARY_PATH=.:/usr/lib:$LD_LIBRARY_PATH
script: cfx test     
